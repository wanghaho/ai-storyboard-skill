---
name: write-continuous-storyboards
description: 根据剧本、角色资产、场景图和最新站位图编写可直接用于AI视频生成的中文分镜提示词，重点处理多人物对白、跨片段连续性、同轴切镜、道具状态和生成错误修复。适用于短剧分镜编写及局部重写。
---

# 连续性分镜提示词

交付可复制的完整中文提示词。已有项目格式时保留原格式；无指定格式时使用 [输出模板](references/output-template.md)。修复生成错误时读取 [错误与修正](references/error-patterns.md)。

## 输入与优先级

最新明确要求 > 最新有效参考及连续状态 > 剧情与正式台词 > 项目规则 > 历史版本。错误生成图仅用于诊断，不自动成为新站位依据。资产编号、角色身份、服装和正式台词不得自行修改或编造。

先提取负责集数、剧情范围、时长、输出片段数、全景上限、人物与声音资产、场景、道具和最新尾帧。缺失非决定性信息时采用合理制作设定并标明；缺失真实资产编号时用人物名或明确待绑定标记，不伪造数字。

## 工作流程

1. 判定每张图的职责：首尾帧继承实际状态；站位图只提取位置、距离、朝向和层级；角色图锁定外貌服装；场景图锁定建筑空间；道具图锁定外形比例。除非用户指定，不继承站位图的动作、景别与构图。白膜、线稿、三视图不继承材质、拼版或标注。
2. 建立空间记录：选择共同观察方向，写每个人的地面位置、前后层级、朝向、距离、遮挡、画外位置，以及门、通道、核验区等固定锚点。定义人物连线形成的轴及摄影机允许所在半区。
3. 建立状态记录：谁用哪只手持什么、道具大小及开合、口袋位置、门灯状态、动作是否完成。每次修改将新状态传播到后续镜头；不要混用旧版状态。
4. 按对白及动作节拍拆片段。默认独立生成片段4—30秒，用户指定优先；各段从0秒计时，小镜头连续无空档。先分配完整台词的实际朗读时间，再安排反应与停顿，不靠倍速或漏词塞进时长。
5. 为每段重复完整资产和独立第0秒站位；画外角色仍要说明位置。禁止仅写“继承上段”“资产同上”。无需每段拍全景，但必须每段写清站位。
6. 编写镜头、动作、口型及尾帧。最后核对剧情覆盖、角色归属、时长、轴线、动线和道具连续性。

## 镜头与空间

- 每镜独立写时间、硬切或动作匹配硬切、焦段、景别、机位高度、拍摄方向、距离、俯仰、运镜和入画清单。使用真实取景范围消除景别用词歧义：例如“头顶至膝盖”属于膝上中景，优先执行用户的取景范围。
- 同轴必须由机位的真实位置保证，仅写“都在大厅一侧”不够；人群看向哪里、镜头朝哪里、背后能见到什么应相互一致。切换不得交换角色左右或把对话对象错误放进说话者身后。
- 同主体相近构图切镜时采用大于30度的有效机位变化或明显景别变化；项目要求更严格时服从项目。不得为凑角度跨轴。用户要求景别跳级时按指定执行。
- 单人镜头通过机位和裁切隔离其他人，不能靠移动其他角色实现。关系镜头遵守全景确定的地面位置，不为了让两个人同框强行挪到一起。
- 走位写起点、屏幕入画边、路线、避让空间、距离与终点。左侧入画不能改成从纵深迎面走来。全景展示所有已在现场的人，不把画外裁切理解成人物消失。
- 站位发生实质变化时，优先在合适位置安排可截图的关系全景；服从用户全景上限和“不切全景”等最新要求。用户指定末尾2秒全景时留足稳定时间，倒数第二镜若指定特写则明确落实。
- 摇摄是原地转动，跟拍是摄影机位移；写运动触发、方向、速度、停点。微推不应被描述成跨越巨大景别。

## 表演、对白与反应

- 每次硬切重新引用当前可见角色资产。说话者姓名、角色编号、服装与音频一一绑定；同段固定音色，情绪只改变气息、语速和重音。
- 连续对白超过约4秒时，在自然语义点插入约1秒反应镜头。按台词对谁造成影响选择反应者：权威证实主角无辜时，优先拍被反驳的质疑者，而非机械插入友方。
- 现场声音跨镜持续，句子不重说；反应者嘴唇闭合，不代说。OS为内心声音，不配口型；画外声不要求说话者入画。无对白段明确无对白、无OS、无旁白及无额外人声。
- 开场立即进入有意义的动作，避免例行长吸气、缓慢抬眼、反复转头。短反应只承载一项变化。用户要求紧凑时先去冗余，不擅自删正式台词。
- 转头和转身区分眼球、头颈、肩胸、髋部与脚步，动作复杂度按时长安排，不把机械动作链堆满每秒。
- 环顾众人默认考虑角色POV跟摇：摄影机在该角色眼位，不能拍到他本人；合理允许其手与道具进入。需拍本人回身展示、再转回操作设备时改用客观机位和动作匹配切镜。不要同时写POV与该角色正面单人镜头。

## 道具连续性

道具同时限定外观和相对人体比例。厘米数若无尺标只作为制作设定，不能声称是图片实测。特写放大构图不放大道具。明确持有手、开合、封面方向、收存口袋及取放全过程；收起后不能下一镜自动举起。操作设备前尾帧朝向与手部运动要衔接下一镜。

## 交付与复核

用户指出错误时先简短说明具体矛盾，再给完整替换提示词；局部修改只交付该范围及必要衔接。不给未经验证的“绝对不会出错”保证。

输出使用自然换行，不插入无意义的斜杠或转义符。负面约束只针对本次风险。未要求文件时在聊天交付；要求文件时遵守指定目录。

检查每段能否独立复制：资产完整；逐人起始位置和画外状态明确；镜头时间连续；英文台词完整且可朗读；反应选对人；说话者不串角；人物和道具不回滚；镜头背景符合方向；全景人数与现实一致；尾帧可接下一段。

此Skill不授权上传剧本、角色图或整个项目。用户另行要求分享时仅发布约定范围；可复用版本用占位符示例，不固化某集人物编号、私人路径或未授权素材。
